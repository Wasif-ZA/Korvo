---
phase: 04-ui-dashboard
plan: 04
type: execute
wave: 2
depends_on: ["04-02"]
files_modified:
  - app/(app)/search/[id]/page.tsx
  - components/app/ContactCard.tsx
  - components/app/EmptyState.tsx
autonomous: false
requirements:
  - UI-01
  - UI-02
  - UI-06
  - UI-07
  - UI-08
  - UI-09
  - DASH-03
must_haves:
  truths:
    - "Search results page uses real contact and draft IDs from PipelineResponse"
    - "Regenerate button in results page calls POST /api/drafts/[id]/regenerate"
    - "Empty states match the copywriting contract exactly"
    - "Mobile layout works on iPhone SE (375px) through desktop"
    - "Warm aesthetic maintained across all authenticated pages"
  artifacts:
    - path: "app/(app)/search/[id]/page.tsx"
      provides: "Results page with real IDs, regenerate handler, onSave handler"
      contains: "handleRegenerate"
    - path: "components/app/EmptyState.tsx"
      provides: "Empty state with correct copy from UI-SPEC"
      contains: "No contacts yet"
  key_links:
    - from: "app/(app)/search/[id]/page.tsx"
      to: "/api/drafts/[id]/regenerate"
      via: "POST call on regenerate button click"
      pattern: "api/drafts.*regenerate"
    - from: "app/(app)/search/[id]/page.tsx"
      to: "/api/drafts/[id]"
      via: "PATCH auto-save from EmailDraft component"
      pattern: "api/drafts"
---

<objective>
Wire search results page to real endpoints for draft editing/regeneration, update empty states to match copywriting contract, and verify mobile responsiveness.

Purpose: The search results page currently uses array-index IDs (c-0, d-0) which break PATCH calls. It also lacks regenerate/save handlers. Empty states need to match the exact copywriting from UI-SPEC. This plan closes the loop on the full search-to-results flow.

Output: Fully functional results page with real draft editing, regeneration, and correct empty state copy. Mobile-verified.
</objective>

<execution_context>
@C:/Users/wasif/OneDrive/Desktop/teehee/03 Projects/Korvo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/wasif/OneDrive/Desktop/teehee/03 Projects/Korvo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-ui-dashboard/04-CONTEXT.md
@.planning/phases/04-ui-dashboard/04-UI-SPEC.md

@app/(app)/search/[id]/page.tsx
@components/app/ContactCard.tsx
@components/app/EmptyState.tsx
@components/app/EmailDraft.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire results page to real endpoints with regenerate and save handlers</name>
  <read_first>app/(app)/search/[id]/page.tsx, components/app/ContactCard.tsx, components/app/EmailDraft.tsx</read_first>
  <files>app/(app)/search/[id]/page.tsx</files>
  <action>
1. Update the `mappedContact` object to use real IDs from PipelineResponse (after Plan 01 adds them):
   - Change `id: \`c-${idx}\`` to `id: contact.id ?? \`c-${idx}\``
   - Change draft `id: \`d-${idx}\`` to `id: draft?.id ?? \`d-${idx}\``

2. Add a `handleRegenerate` function:

   ```typescript
   const handleRegenerate = async (draftId: string) => {
     try {
       const res = await fetch(`/api/drafts/${draftId}/regenerate`, {
         method: "POST",
       });
       if (!res.ok) throw new Error("Failed");
       toast.success("Draft regenerated");
       mutate(); // SWR revalidate to refresh data
     } catch {
       toast.error("Couldn't regenerate draft. Please try again.");
     }
   };
   ```

   Note: The SWR hook in this page is destructured as `{ data, error, isLoading }` — add `mutate` to the destructuring.

3. Add a `handleUpdateDraft` function:

   ```typescript
   const handleUpdateDraft = (
     draftId: string,
     subject: string,
     body: string,
   ) => {
     // EmailDraft auto-save handles the PATCH call internally
     // This callback refreshes the SWR cache after save
     mutate();
   };
   ```

4. Wire these handlers to the ContactCard render:

   ```tsx
   <ContactCard
     key={idx}
     contact={mappedContact}
     onUpdateDraft={handleUpdateDraft}
     onViewDetails={() => setSelectedContactId(contact.id ?? `c-${idx}`)}
     onRegenerateDraft={(draftId) => handleRegenerate(draftId)}
   />
   ```

5. In the SlideOver section, add regenerate handler to EmailDraft:

   ```tsx
   <EmailDraft
     draft={{...}}
     email={selectedContactData.email}
     onClose={() => setSelectedContactId(null)}
     onRegenerate={() => selectedDraft && handleRegenerate(selectedDraft.id ?? "")}
     onSave={() => mutate()}
   />
   ```

6. Update the SlideOver's selectedContactData to use real ID lookup instead of `c-${idx}` matching:
   - Current: `results.contacts.find((c, idx) => \`c-${idx}\` === selectedContactId)`
   - Update: `results.contacts.find((c) => c.id === selectedContactId) || results.contacts.find((c, idx) => \`c-${idx}\` === selectedContactId)`
   - This handles both real IDs (from Plan 01) and fallback array-index IDs
     </action>
     <verify>
     <automated>npx tsc --noEmit 2>&1 | head -20</automated>
     </verify>
     <acceptance_criteria>
   - `grep -n "handleRegenerate" app/(app)/search/[id]/page.tsx` shows regenerate handler
   - `grep -n "api/drafts.*regenerate" app/(app)/search/[id]/page.tsx` shows POST call
   - `grep -n "mutate" app/(app)/search/[id]/page.tsx` shows SWR mutate in destructuring and usage
   - `grep -n "onRegenerateDraft" app/(app)/search/[id]/page.tsx` shows handler passed to ContactCard
   - `grep -n "contact.id" app/(app)/search/[id]/page.tsx` shows real ID usage (with fallback)
   - TypeScript compiles without errors
     </acceptance_criteria>
     <done>Results page uses real IDs, has working regenerate and save handlers, and refreshes SWR cache after mutations.</done>
     </task>

<task type="auto">
  <name>Task 2: Update empty states to match copywriting contract</name>
  <read_first>components/app/EmptyState.tsx, .planning/phases/04-ui-dashboard/04-UI-SPEC.md</read_first>
  <files>components/app/EmptyState.tsx</files>
  <action>
1. Read `components/app/EmptyState.tsx` and verify its copy matches the copywriting contract from UI-SPEC.

2. Per UI-SPEC Empty States table, the dashboard first-load empty state must show:
   - Heading: "No contacts yet"
   - Body: "Run your first search to start building your outreach pipeline."
   - Action: "Start a Search" (primary button linking to `/search`)

3. Update the EmptyState component to match exactly. If the component accepts props for heading/body/action, verify the dashboard passes the correct values. If it's hardcoded, update the hardcoded values to match the contract.

4. Ensure the empty state uses the warm aesthetic:
   - Font: heading in Source Serif 4 (font-serif), body in DM Sans (font-sans)
   - Colors: text-text-primary for heading, text-text-body for body
   - Button: variant="primary" for the action button

5. Also verify that the search results empty state at `app/(app)/search/[id]/page.tsx` (~line 243) uses the correct copy:
   - "No high-resonance contacts identified for this search" is acceptable but per UI-SPEC should be checked
   - The "Try Refined Search" button text is acceptable
     </action>
     <verify>
     <automated>npx tsc --noEmit 2>&1 | head -10</automated>
     </verify>
     <acceptance_criteria>
   - `grep -n "No contacts yet" components/app/EmptyState.tsx` shows correct heading
   - `grep -n "Run your first search" components/app/EmptyState.tsx` shows correct body text
   - `grep -n "Start a Search" components/app/EmptyState.tsx` shows correct action text
   - `grep -n "font-serif" components/app/EmptyState.tsx` shows serif font on heading
   - EmptyState renders primary button linking to /search
     </acceptance_criteria>
     <done>Empty states match the UI-SPEC copywriting contract with correct typography and warm aesthetic.</done>
     </task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Full Phase 4 visual and functional verification</name>
  <files>app/(app)/search/page.tsx, app/(app)/search/[id]/page.tsx, app/(app)/page.tsx</files>
  <action>Human verifies the complete Phase 4 UI across search, results, and dashboard pages.</action>
  <verify>Human approval</verify>
  <done>All 10 verification steps pass and user types "approved".</done>
  <what-built>Full Phase 4 UI and Dashboard: search results with score badges, research toggles, inline draft editing with real auto-save, regenerate button, dashboard Kanban with DnD and optimistic updates, SlideOver detail panels, search history sidebar, follow-up reminders, mobile responsive layout.</what-built>
  <how-to-verify>
    1. Visit /search, enter a company name + role, verify the pipeline tracker shows real-time stage updates
    2. When results load, verify each contact card shows: name, title, email, confidence badge, score badge (colored by tone), hook quote, "View Email Draft" toggle, "View Research" toggle, "Full Details" button
    3. Click "View Email Draft" on a contact — verify subject/body are editable, changes auto-save (look for "Saving..." indicator), Copy to Clipboard works (shows "Copied!" for 2s), Open in Mail opens mailto link, Regenerate button triggers a new draft
    4. Click "View Research" on a contact — verify research card expands with Background / Ask This / Mention This
    5. Click "Full Details" — verify SlideOver opens with score breakdown, research card, email draft
    6. Navigate to dashboard (root of authenticated app), verify: stat cards show real numbers, Kanban board shows real contacts in correct stage columns, search history sidebar shows past searches
    7. Drag a contact between Kanban columns — verify it moves immediately (optimistic), success toast appears, and if you refresh the page the contact is still in the new column
    8. Click a contact in the Kanban — verify SlideOver opens with full details, regenerate works, follow-up reminder can be set
    9. Test on mobile viewport (375px width): search form stacks vertically, Kanban scrolls horizontally, SlideOver is full-screen
    10. Verify warm aesthetic: #FAFAF8 background, Source Serif 4 headings, DM Sans body, orange accent on CTAs only
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- All search results use real DB IDs for contacts and drafts
- Regenerate button calls POST /api/drafts/[id]/regenerate
- Empty states match UI-SPEC copywriting exactly
- Human verification confirms full flow works end-to-end
</verification>

<success_criteria>

- Results page ContactCards use real DB IDs, not array indices
- Regenerate and save handlers work in both results page and dashboard
- Empty states match copywriting contract
- Human approves the full Phase 4 UI via checkpoint
  </success_criteria>

<output>
After completion, create `.planning/phases/04-ui-dashboard/04-04-SUMMARY.md`
</output>
