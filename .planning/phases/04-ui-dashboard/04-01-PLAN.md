---
phase: 04-ui-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/types/agents.ts
  - lib/api/pipeline-response.ts
  - app/api/drafts/[id]/regenerate/route.ts
  - app/api/contacts/[id]/reminder/route.ts
  - components/app/EmailDraft.tsx
  - tests/api/regenerate-route.test.ts
  - tests/api/reminder-route.test.ts
autonomous: true
requirements:
  - EMAIL-05
  - DASH-05
  - UI-02
must_haves:
  truths:
    - "PipelineResponse contacts include real DB IDs so PATCH endpoints work"
    - "PipelineResponse drafts include real DB IDs so draft editing persists"
    - "Regenerate endpoint re-runs Email Drafter and returns new subject+body"
    - "Follow-up reminder persists a reminder date to the database"
    - "No console.log statements remain in production components"
    - "Regenerate and reminder endpoints have vitest coverage"
  artifacts:
    - path: "shared/types/agents.ts"
      provides: "PipelineResponse with id fields on contacts and drafts"
      contains: "id: string"
    - path: "lib/api/pipeline-response.ts"
      provides: "assemblePipelineResponse includes contact.id and outreach.id"
      contains: "id: c.id"
    - path: "app/api/drafts/[id]/regenerate/route.ts"
      provides: "POST endpoint that regenerates a draft email"
      exports: ["POST"]
    - path: "app/api/contacts/[id]/reminder/route.ts"
      provides: "PATCH endpoint that sets/clears follow-up reminder"
      exports: ["PATCH"]
    - path: "tests/api/regenerate-route.test.ts"
      provides: "Vitest tests for regenerate endpoint"
      contains: "describe"
    - path: "tests/api/reminder-route.test.ts"
      provides: "Vitest tests for reminder endpoint"
      contains: "describe"
  key_links:
    - from: "lib/api/pipeline-response.ts"
      to: "shared/types/agents.ts"
      via: "PipelineResponse type import"
      pattern: "import.*PipelineResponse.*agents"
    - from: "app/api/drafts/[id]/regenerate/route.ts"
      to: "lib/agents/email-drafter.ts"
      via: "agent invocation for regeneration"
      pattern: "drafter|emailDraft"
---

<objective>
Complete backend contracts and missing API endpoints needed for full frontend interactivity.

Purpose: The frontend components (EmailDraft, FollowUpReminder, ContactCard) reference PATCH/POST endpoints and real DB IDs that are either missing or use fake array-index IDs. This plan closes those gaps so Wave 2 frontend polish can wire everything cleanly.

Output: Updated PipelineResponse type with real IDs, regenerate endpoint, reminder endpoint, console.log cleanup, vitest coverage for new endpoints.

NOTE: PATCH /api/drafts/[id] already exists (app/api/drafts/[id]/route.ts) with Zod validation + Supabase auth. No need to create it.
</objective>

<execution_context>
@C:/Users/wasif/OneDrive/Desktop/teehee/03 Projects/Korvo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/wasif/OneDrive/Desktop/teehee/03 Projects/Korvo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ui-dashboard/04-CONTEXT.md
@.planning/phases/04-ui-dashboard/04-RESEARCH.md

@shared/types/agents.ts
@lib/api/pipeline-response.ts
@app/api/drafts/[id]/route.ts
@app/api/contacts/[id]/route.ts
@components/app/EmailDraft.tsx

<interfaces>
<!-- Key types the executor needs -->

From shared/types/agents.ts:

```typescript
export interface PipelineResponse {
  company: string;
  role: string;
  pipeline_status: "running" | "complete" | "failed";
  contacts: {
    name: string;
    title: string;
    email: string;
    confidence: number;
    score: number;
    scoreBreakdown: ScoringSignals | null;
    hooks: string[];
  }[];
  steps: { id: string; label: string; status: string; detail: string }[];
  drafts: {
    contact_name: string;
    subject: string;
    body: string;
    hook_used: string;
  }[];
}
```

From lib/api/pipeline-response.ts:

```typescript
export async function assemblePipelineResponse(
  searchId: string,
): Promise<PipelineResponse>;
// Currently maps contacts WITHOUT id field, and drafts WITHOUT id field
// c.id and o.id exist in Prisma query results but are not included in output
```

From app/api/drafts/[id]/route.ts (CONFIRMED EXISTING):

```typescript
// PATCH endpoint for draft auto-save already exists
// Accepts { subject?: string, body?: string }, validates with Zod
// Auth via createSupabaseServerClient + ownership check
// No need to create this endpoint
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add real IDs to PipelineResponse type and assembler</name>
  <read_first>shared/types/agents.ts, lib/api/pipeline-response.ts</read_first>
  <files>shared/types/agents.ts, lib/api/pipeline-response.ts</files>
  <action>
1. In `shared/types/agents.ts`, update PipelineResponse interface:
   - Add `id: string` to the contacts array item type (first field, before `name`)
   - Add `id: string` to the drafts array item type (first field, before `contact_name`)

2. In `lib/api/pipeline-response.ts`, update `assemblePipelineResponse`:
   - In the contacts map at ~line 29, add `id: c.id,` as the first field
   - In the drafts flatMap at ~line 43, add `id: o.id,` as the first field in each draft object

3. Also in `lib/api/pipeline-response.ts`, add `researchBackground`, `researchAskThis`, `researchMentionThis` to the contacts output so the SlideOver in search results can display research data. Add these three fields as string | null after `hooks`.

4. Update PipelineResponse type in agents.ts to match: add `researchBackground: string | null`, `researchAskThis: string | null`, `researchMentionThis: string | null` to the contacts array item type.

Per UI-02: one-click seamless flow requires real IDs so inline draft editing and stage moves work end-to-end without fake array-index IDs.
</action>
<verify>
<automated>npx tsc --noEmit 2>&1 | head -30</automated>
</verify>
<acceptance_criteria> - `grep -n "id: string" shared/types/agents.ts` shows id field in contacts AND drafts sections of PipelineResponse - `grep -n "id: c.id" lib/api/pipeline-response.ts` returns a match - `grep -n "id: o.id" lib/api/pipeline-response.ts` returns a match - `grep -n "researchBackground" shared/types/agents.ts` shows the field in PipelineResponse contacts - TypeScript compiles without errors related to PipelineResponse
</acceptance_criteria>
<done>PipelineResponse type and assembler include real DB IDs for contacts and drafts, plus research fields. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Create regenerate and reminder API endpoints, clean console.logs, add vitest tests</name>
  <read_first>app/api/drafts/[id]/route.ts, app/api/contacts/[id]/route.ts, components/app/EmailDraft.tsx, lib/agents/email-drafter.ts, prisma/schema.prisma, tests/api/search-route.test.ts</read_first>
  <files>app/api/drafts/[id]/regenerate/route.ts, app/api/contacts/[id]/reminder/route.ts, components/app/EmailDraft.tsx, tests/api/regenerate-route.test.ts, tests/api/reminder-route.test.ts</files>
  <action>
1. Create `app/api/drafts/[id]/regenerate/route.ts`:
   - POST handler that accepts outreach ID as route param
   - Auth check via createSupabaseServerClient + getUser
   - Ownership check: outreach.contact.search.userId === user.id
   - Look up the outreach record and its parent contact (need contact.score for tone, contact.name/title, search.company/role)
   - Derive tone from contact.score: 75-100 = "direct", 45-74 = "curious", 0-44 = "value_driven" (per D-09: regenerate keeps same tone derived from score)
   - Import and call the Email Drafter agent function from `lib/agents/email-drafter.ts` (read that file first to understand its function signature)
   - If the drafter agent module does not have a clean re-invocable function, create a simpler approach: use `@anthropic-ai/sdk` directly with Haiku 4.5 to generate a new email draft with the same tone/template. System prompt: "You are a cold email expert. Write a 4-sentence cold email. Tone: {tone}. Template: {templateType}." User message includes contact name, title, company, role, hook.
   - Update the outreach record with new subject + body via prisma.outreach.update
   - Return `{ success: true, data: { id, subject, body, hook_used } }`
   - Validate with Zod (no body needed, but validate route param exists)

2. Create `app/api/contacts/[id]/reminder/route.ts`:
   - PATCH handler that accepts contact ID
   - Auth check via createSupabaseServerClient + getUser
   - Ownership check: contact.search.userId === user.id
   - Zod body schema: `{ reminderActive: boolean }`
   - If reminderActive=true: store reminder as JSON in contact.notes field: `{ "reminder_at": "<ISO date 7 days from now>" }`. Use prisma.contact.update to set notes field.
   - If reminderActive=false: clear by setting notes to null
   - Return `{ success: true, data: { id, reminderAt } }`
   - Per DASH-05: basic scaffolded persistence, full implementation deferred to V3

3. Clean `components/app/EmailDraft.tsx`:
   - Remove `console.log(\`[mock-save]\`)` at ~line 47
   - Remove `console.warn("Draft auto-save failed")` at ~line 60 — replace with silent failure (hasUnsavedChanges stays true so user sees "Unsaved changes" label)
   - Per coding-style rule: no console.log in production code

4. Create `tests/api/regenerate-route.test.ts`:
   - Read `tests/api/search-route.test.ts` first to follow established test patterns (mock setup, Supabase auth mocking, Prisma mocking)
   - Test cases:
     a. Returns 401 when no auth token
     b. Returns 404 when outreach not found or not owned by user
     c. Returns 200 with new subject/body on success
     d. Verifies prisma.outreach.update is called with new draft content
   - Mock the Anthropic SDK call (or drafter agent function) to return deterministic output

5. Create `tests/api/reminder-route.test.ts`:
   - Follow same test patterns as regenerate test
   - Test cases:
     a. Returns 401 when no auth token
     b. Returns 400 when body fails Zod validation (missing reminderActive)
     c. Returns 404 when contact not found or not owned
     d. Returns 200 and sets reminder when reminderActive=true
     e. Returns 200 and clears reminder when reminderActive=false
     </action>
     <verify>
     <automated>npx tsc --noEmit 2>&1 | head -30 && npx vitest run tests/api/regenerate-route.test.ts tests/api/reminder-route.test.ts --reporter=verbose 2>&1 | tail -30</automated>
     </verify>
     <acceptance_criteria>
   - File `app/api/drafts/[id]/regenerate/route.ts` exists and exports a POST function
   - `grep -n "export async function POST" app/api/drafts/[id]/regenerate/route.ts` returns a match
   - File `app/api/contacts/[id]/reminder/route.ts` exists and exports a PATCH function
   - `grep -n "export async function PATCH" app/api/contacts/[id]/reminder/route.ts` returns a match
   - `grep -rn "console.log" components/app/EmailDraft.tsx` returns NO matches
   - `grep -rn "console.warn" components/app/EmailDraft.tsx` returns NO matches
   - Both endpoints use Zod validation and Supabase auth check
   - TypeScript compiles without errors
   - Vitest tests pass for both regenerate and reminder routes
     </acceptance_criteria>
     <done>Regenerate endpoint re-runs drafter agent and returns new draft. Reminder endpoint persists reminder date in contact.notes. No console.log/warn in EmailDraft.tsx. Both endpoints have vitest coverage.</done>
     </task>

</tasks>

<verification>
- `npx tsc --noEmit` passes (no type errors)
- `npx vitest run tests/api/regenerate-route.test.ts tests/api/reminder-route.test.ts` passes
- `grep -rn "console.log\|console.warn" components/app/EmailDraft.tsx` returns nothing
- `grep "id: c.id" lib/api/pipeline-response.ts` confirms real contact IDs in response
- `grep "id: o.id" lib/api/pipeline-response.ts` confirms real draft IDs in response
- Both new route files exist and export correct HTTP methods
- PATCH /api/drafts/[id] already exists (confirmed) — no action needed
</verification>

<success_criteria>

- PipelineResponse includes real DB IDs on contacts and drafts
- POST /api/drafts/[id]/regenerate creates a new draft via the drafter agent
- PATCH /api/contacts/[id]/reminder persists a reminder date
- No console.log or console.warn in EmailDraft.tsx
- TypeScript compiles cleanly
- Vitest tests pass for regenerate and reminder endpoints
  </success_criteria>

<output>
After completion, create `.planning/phases/04-ui-dashboard/04-01-SUMMARY.md`
</output>
