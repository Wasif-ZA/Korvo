---
phase: 01-foundation
plan: 04
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - app/page.tsx
  - app/pricing/page.tsx
  - components/nav/NavBar.tsx
  - components/pricing/PricingCard.tsx
  - components/pricing/PricingToggle.tsx
  - components/pricing/PromoCodeInput.tsx
  - app/layout.tsx
autonomous: true
requirements: [AUTH-02, PAY-05]

must_haves:
  truths:
    - "Landing page renders with hero, how-it-works, pricing section, and footer CTA"
    - "Search bar on landing page has 3 fields (company, role, location) and a search button"
    - "Pricing section shows Free and Pro plans with correct AUD pricing"
    - "Annual/monthly toggle switches price display without page reload"
    - "/pricing page renders with same pricing cards plus promo code input"
    - "NavBar shows on all pages with logo and CTA"
    - "PricingCard ctaAction wires to /api/stripe/checkout with selected priceId and optional promoCode"
  artifacts:
    - path: "app/page.tsx"
      provides: "Landing page with hero, how-it-works, pricing section"
      min_lines: 80
    - path: "app/pricing/page.tsx"
      provides: "Dedicated pricing page"
      min_lines: 40
    - path: "components/nav/NavBar.tsx"
      provides: "Sticky navigation bar"
      exports: ["NavBar"]
    - path: "components/pricing/PricingCard.tsx"
      provides: "Pricing card component for Free and Pro plans"
      exports: ["PricingCard"]
    - path: "components/pricing/PricingToggle.tsx"
      provides: "Monthly/Annual toggle"
      exports: ["PricingToggle"]
  key_links:
    - from: "app/page.tsx"
      to: "components/pricing/PricingCard.tsx"
      via: "import and render in pricing section"
      pattern: "PricingCard"
    - from: "app/layout.tsx"
      to: "components/nav/NavBar.tsx"
      via: "NavBar rendered in root layout"
      pattern: "NavBar"
    - from: "components/pricing/PricingCard.tsx"
      to: "/api/stripe/checkout"
      via: "ctaAction callback POSTs priceId + promoCode to checkout API, redirects to returned Stripe Checkout URL"
      pattern: "api/stripe/checkout"
---

<objective>
Build the landing page (hero, search bar, how-it-works, pricing section, footer CTA) and the dedicated /pricing page with monthly/annual toggle and promo code support.

Purpose: The landing page is the user's first impression and contains the search entry point. The pricing page is needed for the upgrade flow (D-07: pricing shown in both landing page section AND /pricing page). These are the public-facing pages that drive signups and conversions.
Output: Complete landing page and /pricing page matching the UI-SPEC and copywriting contract.
</objective>

<execution_context>
@C:/Users/wasif/OneDrive/Desktop/teehee/03 Projects/Korvo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/wasif/OneDrive/Desktop/teehee/03 Projects/Korvo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-UI-SPEC.md

<interfaces>
<!-- From Plan 01 -->
From components/ui/Button.tsx:
export function Button({ className, variant, size, ...props }: ButtonProps): JSX.Element
// Variants: "primary" | "secondary" | "ghost" | "destructive"

From components/ui/Input.tsx:
export function Input({ className, error, ...props }: InputProps): JSX.Element

From components/ui/Card.tsx:
export function Card({ className, highlighted, children, ...props }: CardProps): JSX.Element

From lib/utils/cn.ts:
export function cn(...inputs: ClassValue[]): string

<!-- From Plan 05 (consumed by ctaAction wiring) -->
From app/api/stripe/checkout/route.ts:
// POST { priceId: string, promoCode?: string } -> { url: string }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create NavBar component and update root layout</name>
  <files>components/nav/NavBar.tsx, app/layout.tsx</files>
  <read_first>.planning/phases/01-foundation/01-UI-SPEC.md (Nav Bar section, Color section, Typography section), app/layout.tsx (existing from Plan 01), components/ui/Button.tsx (import path)</read_first>
  <action>
    1. Create `components/nav/NavBar.tsx`:
    - Height: 64px (`h-16`)
    - Background: `bg-[#FAFAF8]`
    - Border bottom: `border-b border-[#E5E4E0]`
    - Sticky top: `sticky top-0 z-50`
    - Max content width: `max-w-[1200px] mx-auto`
    - Horizontal padding: `px-4 sm:px-6 lg:px-8`
    - Layout: flex justify-between items-center
    - Left: Logo text "Korvo" in `text-xl font-semibold text-[#1C1C1A]` (link to `/`)
    - Right: nav links + CTA
      - Pre-auth state (default): "Pricing" link (ghost style, href="/pricing") + "Get started" Button (primary variant, href="/")
      - Post-auth state: "Dashboard" link + user avatar/name
      - For now, render pre-auth state. Auth-aware switching will be added when auth components are wired in Plan 06.
    - Nav links: `text-sm font-semibold text-[#1C1C1A]` with `hover:text-teal-600` transition
    - Active link: `text-teal-600` with underline indicator (2px bottom border teal-600)
    - Mobile: hamburger menu icon (lucide-react `Menu` icon) toggles a dropdown with links stacked vertically

    2. Update `app/layout.tsx`:
    - Import and render `<NavBar />` above `{children}` in the body
    - Keep existing Geist font, Toaster, and metadata from Plan 01

    Per UI-SPEC: "Logo: left. Nav links (center or right depending on auth state). CTA button (rightmost)."
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `components/nav/NavBar.tsx` exports `NavBar`
    - NavBar has `sticky top-0 z-50` classes
    - NavBar has `bg-[#FAFAF8]` and `border-b border-[#E5E4E0]`
    - NavBar contains "Korvo" text as logo
    - NavBar contains "Get started" Button with primary variant
    - NavBar contains link to `/pricing`
    - `app/layout.tsx` imports and renders `<NavBar />`
    - Mobile hamburger uses `Menu` icon from lucide-react
  </acceptance_criteria>
  <done>NavBar renders on all pages via root layout, shows logo, pricing link, and primary CTA, with mobile hamburger menu.</done>
</task>

<task type="auto">
  <name>Task 2: Build landing page with hero, search bar, how-it-works, pricing section, and footer CTA</name>
  <files>app/page.tsx, components/pricing/PricingCard.tsx, components/pricing/PricingToggle.tsx</files>
  <read_first>.planning/phases/01-foundation/01-UI-SPEC.md (ALL sections — Copywriting Contract: Landing Page, Layout section, Component Inventory: Input/Search Bar, Color, Spacing Scale, Typography, Interaction Contract: Annual/Monthly Pricing Toggle)</read_first>
  <action>
    1. Create `components/pricing/PricingToggle.tsx`:
    - Pill-style toggle with "Monthly" and "Annually" options
    - "Monthly" is default selected state
    - Selected pill: `bg-white shadow-sm text-[#1C1C1A] font-semibold`
    - Unselected pill: `text-gray-500`
    - Container: `bg-[#F4F3F0] rounded-full p-1 inline-flex`
    - Each pill: `px-4 py-2 rounded-full text-sm transition-colors duration-150`
    - Props: `interval: 'monthly' | 'annually'`, `onToggle: (interval) => void`
    - When "Annually" selected, update price display AND CTA label simultaneously, no page reload

    2. Create `components/pricing/PricingCard.tsx`:
    - Props: `planName: string`, `tagline: string`, `price: string`, `interval: string`, `features: string[]`, `ctaText: string`, `ctaAction: () => void`, `highlighted?: boolean`, `badge?: string`
    - Uses Card component from `components/ui/Card.tsx`
    - If `highlighted`: Card with `highlighted` prop (border-2 border-teal-600 bg-white), badge "Most popular" in bg-teal-600 text-white text-xs px-2 py-1 rounded-full
    - Plan name: `text-2xl font-semibold`
    - Tagline: `text-sm text-gray-500`
    - Price: `text-5xl font-semibold` with `/month` or `/year` in `text-base text-gray-500`
    - Features list: checkmark icon (lucide-react `Check`) + feature text in `text-sm text-gray-500`, vertical gap of 12px
    - CTA: full-width Button (primary for Pro, secondary for Free)
    - If badge for annual: "Save 35%" badge next to price in `bg-teal-100 text-teal-700 text-xs px-2 py-0.5 rounded-full`

    3. Replace `app/page.tsx` with full landing page:

    **Hero section** (py-16 lg:py-24):
    - Max width container: `max-w-[1200px] mx-auto px-4 sm:px-6 lg:px-8`
    - Headline: "Land interviews with one search." — `text-5xl font-semibold leading-none text-[#1C1C1A]` on desktop, `text-4xl` on mobile
    - Subheadline: "Type a company name. Get 3 contacts with personalized cold emails ready to send." — `text-base text-gray-500 mt-4 max-w-xl`
    - Search bar group below subheadline (mt-8):
      - Desktop (lg+): inline row of 3 Input fields (Company, Role, Location) + primary Button "Start for free", all in a flex row with gap-3
      - Tablet (md): 2x2 grid
      - Mobile (<md): stacked vertically, each full width
      - Company Input: placeholder "e.g. Atlassian", required
      - Role Input: placeholder "e.g. Software Engineer", required
      - Location Input: placeholder "e.g. Sydney" (optional)
      - Search button: primary variant, "Start for free", size lg
      - Hero search inputs use the 52px height variant (`h-[52px]`)
      - Form action: currently just prevents default (search API wired in Plan 06)
    - Secondary CTA below search bar: "See how it works" ghost Button, onClick scrolls to how-it-works section

    **How-it-works section** (py-16 lg:py-24, id="how-it-works"):
    - Heading: "Three steps to your next coffee chat" — `text-2xl font-semibold text-center`
    - 3 cards in a row on desktop (lg:grid-cols-3 gap-6), stacked on mobile
    - Each card (Card component):
      - Step number badge: "1", "2", "3" in `bg-teal-600 text-white w-8 h-8 rounded-full flex items-center justify-center text-sm font-semibold`
      - Title: per UI-SPEC copy ("Search a company", "Get 3 contacts with emails", "Send a personalized email")
      - Description: per UI-SPEC copy
      - Title: `text-lg font-semibold mt-4`
      - Description: `text-sm text-gray-500 mt-2`

    **Pricing section** (py-16 lg:py-24, bg-[#FAFAF8]):
    - Heading: "Simple pricing. No surprises." — `text-2xl font-semibold text-center`
    - Subheading: "Start free. Upgrade when you're ready." — `text-base text-gray-500 text-center mt-2`
    - PricingToggle centered below subheading (mt-6)
    - Two PricingCards side by side (lg:grid-cols-2 gap-6, max-w-3xl mx-auto, mt-8):
      - Free: "Free", "Get a feel for it", "AUD $0", features per UI-SPEC, CTA "Get started for free" (secondary)
      - Pro: highlighted, "Pro", "For serious outreach", monthly: "AUD $19 / month", annual: "AUD $149 / year" + "Save 35%" badge, features per UI-SPEC, CTA "Start Pro" (primary)
    - Use React state for toggle interval, switch price/CTA dynamically
    - **ctaAction wiring for Pro card:** The ctaAction callback must POST to `/api/stripe/checkout` with the selected `priceId` (monthly: `STRIPE_PRO_MONTHLY_PRICE_ID`, annual: `STRIPE_PRO_ANNUAL_PRICE_ID`) and redirect to the returned Stripe Checkout URL. Since env vars for price IDs are server-side only, pass interval as param and let the API resolve the price ID. For now, ctaAction should call: `fetch('/api/stripe/checkout', { method: 'POST', body: JSON.stringify({ priceId: interval === 'monthly' ? 'monthly' : 'annual' }) }).then(r => r.json()).then(d => window.location.href = d.url)`. The checkout route (Plan 05) will resolve the actual Stripe price ID from env vars.
    - **ctaAction for Free card:** Navigate to `/` (scroll to search bar) or no-op if already on landing page.

    **Social proof** (small section):
    - "Built by a UTS student who landed 12 coffee chats." — `text-sm text-gray-500 text-center mt-8`

    **Footer CTA section** (py-16):
    - "Start your outreach for free" — `text-2xl font-semibold text-center`
    - Primary Button "Get started" centered below (mt-4)

    **Footer** (py-8 border-t border-[#E5E4E0]):
    - "(c) 2026 Korvo" left, "Pricing" and "Settings" links right
    - `text-sm text-gray-500`

    Per D-07: Pricing shown in both landing page section AND dedicated /pricing page.
    Per D-09: Both monthly and annual pricing tiers.
    Per D-11: Pricing in AUD.
    Per D-18: Pure Tailwind CSS, no component library.
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `app/page.tsx` contains "Land interviews with one search."
    - `app/page.tsx` contains 3 Input fields for Company, Role, Location
    - `app/page.tsx` contains "Three steps to your next coffee chat"
    - `app/page.tsx` contains "Simple pricing. No surprises."
    - `app/page.tsx` contains "AUD $19" and "AUD $149"
    - `components/pricing/PricingCard.tsx` exports `PricingCard` with `highlighted` prop
    - `components/pricing/PricingCard.tsx` ctaAction prop is called onClick of CTA button
    - `components/pricing/PricingToggle.tsx` exports `PricingToggle` with `interval` and `onToggle` props
    - `components/pricing/PricingToggle.tsx` contains "Monthly" and "Annually" text
    - Pro card ctaAction wires to `/api/stripe/checkout` with priceId
    - Build succeeds without errors
  </acceptance_criteria>
  <done>Landing page renders all sections (hero + search bar + how-it-works + pricing + footer), pricing toggle switches between monthly/annual without reload, Pro CTA wired to Stripe checkout API, all copy matches UI-SPEC.</done>
</task>

<task type="auto">
  <name>Task 3: Build dedicated /pricing page with promo code support</name>
  <files>app/pricing/page.tsx, components/pricing/PromoCodeInput.tsx</files>
  <read_first>.planning/phases/01-foundation/01-UI-SPEC.md (Copywriting Contract: Pricing Page section), components/pricing/PricingCard.tsx (from Task 2), components/pricing/PricingToggle.tsx (from Task 2)</read_first>
  <action>
    1. Create `components/pricing/PromoCodeInput.tsx`:
    - Label: "Have a promo code?" in `text-sm font-semibold text-[#1C1C1A]`
    - Input field using Input component, placeholder "e.g. UTSBDSOC"
    - "Apply" ghost Button next to input
    - State: `code` string, `applied` boolean, `error` string
    - On apply: store code in component state (actual Stripe integration in Plan 05)
    - When applied: show success state with green check "Code applied" text
    - When error: show error via Input error prop
    - Props: `onCodeApplied: (code: string) => void`

    2. Create `app/pricing/page.tsx`:
    - Page heading: "Pricing" — `text-5xl font-semibold text-center` (Display size per UI-SPEC)
    - Subheading: "Free to start. Pro when you mean business." — `text-base text-gray-500 text-center mt-4`
    - PricingToggle centered (mt-8)
    - Two PricingCards (same as landing page pricing section) (mt-8, lg:grid-cols-2 gap-6, max-w-3xl mx-auto)
    - PromoCodeInput below pricing cards (mt-8, centered, max-w-sm mx-auto)
    - **ctaAction wiring:** Same as landing page — Pro card ctaAction POSTs to `/api/stripe/checkout` with `{ priceId, promoCode }`. The `promoCode` comes from PromoCodeInput state (passed up via onCodeApplied callback). When a promo code is applied, pass it along: `fetch('/api/stripe/checkout', { method: 'POST', body: JSON.stringify({ priceId, promoCode: appliedCode }) })`.
    - Set page metadata: `title: "Pricing -- Korvo"`, `description: "Simple pricing. Free to start. Pro when you mean business."`
    - Page is a client component (`'use client'`) for toggle state management
    - Max width container: `max-w-[1200px] mx-auto px-4 sm:px-6 lg:px-8`
    - Section padding: `py-16 lg:py-24`

    Per D-07: Dedicated /pricing page with same plans.
    Per D-10: Promo/discount codes supported (UTSBDSOC for 50% off first month).
    Per D-11: AUD pricing.
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `app/pricing/page.tsx` contains "Pricing" heading and "Free to start. Pro when you mean business." subheading
    - `app/pricing/page.tsx` imports and renders `PricingCard`, `PricingToggle`, and `PromoCodeInput`
    - `app/pricing/page.tsx` contains metadata with `title: "Pricing`
    - `app/pricing/page.tsx` wires ctaAction to `/api/stripe/checkout` with promoCode when applied
    - `components/pricing/PromoCodeInput.tsx` exports `PromoCodeInput`
    - `components/pricing/PromoCodeInput.tsx` contains placeholder "e.g. UTSBDSOC"
    - `components/pricing/PromoCodeInput.tsx` has `onCodeApplied` callback prop
    - Build succeeds
  </acceptance_criteria>
  <done>Dedicated /pricing page renders with heading, toggle, both plan cards, and promo code input. Pro CTA wired to Stripe checkout API with promo code pass-through. Promo code UI stores code locally (Stripe wiring in Plan 05).</done>
</task>

</tasks>

<verification>
1. `npx next build` — builds successfully
2. Landing page has all 5 sections: hero, search bar, how-it-works, pricing, footer
3. /pricing page has pricing cards with toggle and promo code input
4. All copy matches UI-SPEC copywriting contract exactly
5. Pricing shows AUD currency
6. Pro card ctaAction wires to /api/stripe/checkout with priceId and optional promoCode
7. Mobile responsive (no horizontal scroll on 375px viewport)
</verification>

<success_criteria>
- Landing page matches UI-SPEC with hero headline, 3-field search bar, how-it-works steps, pricing cards
- /pricing page shows Free and Pro plans with AUD pricing
- Monthly/annual toggle switches pricing display dynamically
- Pro card CTA wired to /api/stripe/checkout with correct priceId and optional promoCode
- Promo code input accepts codes and passes them to checkout API
- NavBar renders on all pages with logo and navigation
- All pages build without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
