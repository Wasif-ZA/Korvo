---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - app/layout.tsx
  - app/page.tsx
  - app/globals.css
  - lib/utils/cn.ts
  - components/ui/Button.tsx
  - components/ui/Input.tsx
  - components/ui/Card.tsx
  - components/ui/Modal.tsx
  - vitest.config.ts
  - .env.local.example
  - proxy.ts
autonomous: true
requirements: [FOUND-01]
user_setup:
  - service: supabase
    why: "Database + Auth provider"
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Supabase Dashboard -> Settings -> API -> Project URL"
      - name: NEXT_PUBLIC_SUPABASE_ANON_KEY
        source: "Supabase Dashboard -> Settings -> API -> anon public key"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard -> Settings -> API -> service_role key (secret)"

must_haves:
  truths:
    - "Next.js 16 dev server starts without errors"
    - "TailwindCSS 4 utility classes render correctly"
    - "Design system components (Button, Input, Card, Modal) render with correct variants"
    - "cn() utility merges Tailwind classes without conflicts"
    - "Vitest runs and passes"
  artifacts:
    - path: "package.json"
      provides: "All Phase 1 dependencies installed"
      contains: "next"
    - path: "lib/utils/cn.ts"
      provides: "Tailwind class merge utility"
      exports: ["cn"]
    - path: "components/ui/Button.tsx"
      provides: "Button with primary/secondary/ghost/destructive variants"
      exports: ["Button"]
    - path: "vitest.config.ts"
      provides: "Test framework configuration"
      contains: "vitest"
  key_links:
    - from: "components/ui/Button.tsx"
      to: "lib/utils/cn.ts"
      via: "import { cn }"
      pattern: "import.*cn.*from.*lib/utils/cn"
---

<objective>
Scaffold the Next.js 16 project with all Phase 1 dependencies, create the design system base components per the UI-SPEC, set up Vitest, and configure the environment template.

Purpose: Everything else in Phase 1 depends on the project existing with the correct stack. The design system components are needed by Plans 03, 04, 05, and 06.
Output: A running Next.js 16 dev server with TailwindCSS 4, base UI components, Vitest config, and env template.
</objective>

<execution_context>
@C:/Users/wasif/OneDrive/Desktop/teehee/03 Projects/Korvo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/wasif/OneDrive/Desktop/teehee/03 Projects/Korvo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 16 project and install all Phase 1 dependencies</name>
  <files>package.json, tsconfig.json, app/layout.tsx, app/page.tsx, app/globals.css, .env.local.example, proxy.ts</files>
  <read_first>.planning/phases/01-foundation/01-RESEARCH.md (Standard Stack + Installation sections), .planning/phases/01-foundation/01-UI-SPEC.md (Design System + Typography + Color sections)</read_first>
  <action>
    1. Run `npx create-next-app@latest korvo --typescript --tailwind --app --turbopack` in the project root's parent directory, then move contents into the current project root (or run in-place if the directory is empty except for .planning and .claude).

    2. Install all Phase 1 dependencies:
    ```
    npm install prisma @prisma/client @supabase/supabase-js @supabase/ssr stripe zod class-variance-authority clsx tailwind-merge lucide-react react-hot-toast
    npm install -D vitest @vitest/coverage-v8 @types/node prettier eslint
    ```

    3. Update `app/globals.css` to include the custom theme tokens from the UI-SPEC. TailwindCSS 4 uses CSS-native config via `@theme` directive. Add:
    - Custom colors: `--color-warm-white: #FAFAF8`, `--color-warm-muted: #F4F3F0`, `--color-border: #E5E4E0`, `--color-text-primary: #1C1C1A`
    - Set body background to `#FAFAF8`, font-family to Geist Sans (loaded via `next/font/google` in layout.tsx)
    - Default text color `#1C1C1A`

    4. Update `app/layout.tsx`:
    - Import Geist font from `next/font/google` with weights 400 and 600
    - Apply font class to html element
    - Add `<Toaster />` from `react-hot-toast`
    - Set metadata: title "Korvo — Land interviews with one search", description matching hero subheadline

    5. Create `app/page.tsx` as a minimal placeholder: export default function rendering "Korvo" heading (will be replaced in Plan 04).

    6. Create `proxy.ts` at project root (NOT middleware.ts per Next.js 16):
    - Export `proxy` function that passes through all requests (auth logic added in Plan 03)
    - Export `config` with matcher excluding static files
    - Add comment: `// Auth guard logic will be added in Plan 03`

    7. Create `.env.local.example` with all required env vars (no real values):
    ```
    NEXT_PUBLIC_SUPABASE_URL=
    NEXT_PUBLIC_SUPABASE_ANON_KEY=
    SUPABASE_SERVICE_ROLE_KEY=
    DATABASE_URL=
    DIRECT_DATABASE_URL=
    STRIPE_SECRET_KEY=
    STRIPE_WEBHOOK_SECRET=
    STRIPE_PRO_MONTHLY_PRICE_ID=
    STRIPE_PRO_ANNUAL_PRICE_ID=
    NEXT_PUBLIC_BASE_URL=http://localhost:3000
    ```

    Per D-17: Single repo structure. Create empty `shared/` and `worker/` directories with `.gitkeep` files.
    Per D-20: Environment variables via .env files.
  </action>
  <verify>
    <automated>npx next build 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains `"next"`, `"prisma"`, `"@supabase/ssr"`, `"stripe"`, `"zod"`, `"class-variance-authority"`, `"vitest"`
    - `app/globals.css` contains `--color-warm-white` and `#FAFAF8`
    - `app/layout.tsx` contains `Geist` font import and `Toaster`
    - `proxy.ts` exists at project root with `export async function proxy`
    - `.env.local.example` contains `NEXT_PUBLIC_SUPABASE_URL` and `STRIPE_SECRET_KEY`
    - `npm run dev` starts without errors
  </acceptance_criteria>
  <done>Next.js 16 project runs with all Phase 1 deps installed, TailwindCSS 4 custom theme tokens active, proxy.ts stub in place, env template created.</done>
</task>

<task type="auto">
  <name>Task 2: Create design system base components (Button, Input, Card, Modal) and cn() utility</name>
  <files>lib/utils/cn.ts, components/ui/Button.tsx, components/ui/Input.tsx, components/ui/Card.tsx, components/ui/Modal.tsx</files>
  <read_first>.planning/phases/01-foundation/01-UI-SPEC.md (Component Inventory section — Button variants, Input specs, Card specs, Modal Overlay specs), .planning/phases/01-foundation/01-RESEARCH.md (Button Component with CVA code example, cn() Utility code example)</read_first>
  <action>
    1. Create `lib/utils/cn.ts`:
    ```typescript
    import { clsx, type ClassValue } from 'clsx'
    import { twMerge } from 'tailwind-merge'
    export function cn(...inputs: ClassValue[]) { return twMerge(clsx(inputs)) }
    ```

    2. Create `components/ui/Button.tsx` using CVA with exact specs from UI-SPEC:
    - Variants: `primary` (bg-teal-600, text-white, hover:bg-teal-700), `secondary` (bg-[#F4F3F0], text-[#1C1C1A], border border-[#E5E4E0], hover:bg-[#E9E8E4]), `ghost` (transparent, text-[#1C1C1A], hover:bg-[#F4F3F0]), `destructive` (bg-red-600, text-white, hover:bg-red-700)
    - Size: `default` (h-11 px-4 — 44px touch target), `lg` (h-12 px-6)
    - States: focus-visible:ring-2 ring-teal-600 ring-offset-2, active:scale-[0.97], disabled:opacity-50 disabled:cursor-not-allowed
    - Border radius: rounded-lg (8px)
    - Transition: transition-colors duration-150 ease-in-out
    - Accept `isLoading` prop that shows spinner and disables button

    3. Create `components/ui/Input.tsx`:
    - Height: 44px default (h-11), 52px hero variant (h-[52px])
    - Border: border border-[#E5E4E0], rounded-lg
    - Focus: focus:border-teal-600 focus:ring-2 focus:ring-teal-600/50
    - Error state: `error` prop that sets border-red-600 and renders error message below in text-red-600 text-sm
    - Disabled: bg-[#F4F3F0] cursor-not-allowed
    - Placeholder: text-gray-400

    4. Create `components/ui/Card.tsx`:
    - Border radius: rounded-xl (12px)
    - Background: bg-[#F4F3F0]
    - Border: border border-[#E5E4E0]
    - Padding: p-6 (24px)
    - Shadow: shadow-sm
    - Accept `highlighted` prop for Pro pricing card: border-2 border-teal-600 bg-white

    5. Create `components/ui/Modal.tsx`:
    - Backdrop: fixed inset-0 bg-black/40 z-50
    - Modal card: centered, max-w-[440px], bg-white, rounded-2xl, p-8
    - Accept `dismissable` prop (default false per D-03 guest modal behavior)
    - If dismissable=true, backdrop click calls onClose
    - Transition: opacity 200ms, scale from 0.95 to 1.0
    - Mobile: bottom sheet pattern on screens below 768px (rounded-t-2xl, fixed bottom-0, w-full)
    - Accept `children` for content

    All components must use `cn()` for class composition and export named exports.
    Per D-18: Pure Tailwind CSS, no component library.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `lib/utils/cn.ts` exports `cn` function
    - `components/ui/Button.tsx` exports `Button` with `variant` prop accepting `"primary" | "secondary" | "ghost" | "destructive"`
    - `components/ui/Button.tsx` contains `bg-teal-600` and `hover:bg-teal-700`
    - `components/ui/Input.tsx` exports `Input` with `error` prop
    - `components/ui/Card.tsx` exports `Card` with `highlighted` prop
    - `components/ui/Modal.tsx` exports `Modal` with `dismissable` prop defaulting to `false`
    - `components/ui/Modal.tsx` contains `bg-black/40` for backdrop
    - TypeScript compiles with zero errors
  </acceptance_criteria>
  <done>All 4 base UI components render with correct variants, match the UI-SPEC color/spacing/interaction contract, and compile without type errors.</done>
</task>

<task type="auto">
  <name>Task 3: Set up Vitest config with test utilities and smoke tests</name>
  <files>vitest.config.ts, tests/setup.ts, tests/ui/button.test.tsx, tests/utils/cn.test.ts</files>
  <read_first>.planning/phases/01-foundation/01-VALIDATION.md (Wave 0 Requirements, Test Infrastructure), .planning/phases/01-foundation/01-RESEARCH.md (Validation Architecture section)</read_first>
  <action>
    1. Create `vitest.config.ts`:
    ```typescript
    import { defineConfig } from 'vitest/config'
    import path from 'path'

    export default defineConfig({
      test: {
        globals: true,
        environment: 'node',
        setupFiles: ['./tests/setup.ts'],
        coverage: {
          provider: 'v8',
          reporter: ['text', 'lcov'],
          exclude: ['node_modules/', '.next/', 'tests/'],
        },
      },
      resolve: {
        alias: {
          '@': path.resolve(__dirname, '.'),
        },
      },
    })
    ```

    2. Create `tests/setup.ts` with shared mock stubs:
    - Mock `@supabase/ssr` with `createServerClient` and `createBrowserClient` returning mock Supabase client objects
    - Mock `stripe` module returning a mock Stripe instance
    - These mocks will be refined in later plans as needed

    3. Create `tests/utils/cn.test.ts`:
    - Test that cn() merges classes: `cn('p-4', 'p-6')` returns `'p-6'` (tailwind-merge deduplication)
    - Test that cn() handles conditional classes: `cn('base', false && 'hidden')` returns `'base'`
    - Test that cn() handles arrays: `cn(['p-4', 'mt-2'])` returns `'p-4 mt-2'`

    4. Create `tests/ui/button.test.tsx`:
    - Import Button component
    - Test that it renders without crashing (requires jsdom environment for this file — add `// @vitest-environment jsdom` comment)
    - Install `@testing-library/react` and `@testing-library/jest-dom` and `jsdom` as dev dependencies
    - Test primary variant has teal-600 class
    - Test disabled state has opacity-50 class

    Add `"test": "vitest run --reporter=verbose"` and `"test:coverage": "vitest run --coverage"` to package.json scripts.
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `vitest.config.ts` contains `provider: 'v8'` for coverage
    - `tests/setup.ts` exists with Supabase and Stripe mock stubs
    - `npx vitest run` executes and all tests pass (exit code 0)
    - `tests/utils/cn.test.ts` has at least 3 test cases
    - `package.json` contains `"test": "vitest run --reporter=verbose"`
  </acceptance_criteria>
  <done>Vitest runs, cn() utility tests pass, Button smoke test passes, test infrastructure ready for Plans 02-06 to add domain-specific tests.</done>
</task>

</tasks>

<verification>
1. `npm run dev` starts Next.js 16 dev server on port 3000
2. `npx tsc --noEmit` — zero TypeScript errors
3. `npx vitest run` — all tests pass
4. Design system components import and render without errors
</verification>

<success_criteria>
- Next.js 16.2.2 project running with TailwindCSS 4.2.2, TypeScript strict mode
- All Phase 1 npm dependencies installed (prisma, @supabase/ssr, stripe, zod, cva, etc.)
- 4 base UI components (Button, Input, Card, Modal) matching UI-SPEC exactly
- cn() utility working with tailwind-merge
- Vitest configured with coverage, tests passing
- proxy.ts stub in place (auth logic added in Plan 03)
- .env.local.example documenting all required env vars
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
