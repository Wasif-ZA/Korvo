---
phase: 01-foundation
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
  - components/ui/Button.tsx
  - app/page.tsx
  - vitest.config.ts
autonomous: true
gap_closure: true
requirements:
  - FOUND-01

must_haves:
  truths:
    - "The app renders a warm off-white background (#FAFAF8), not a near-black (#0A0A0A) background"
    - "Button primary variant uses bg-teal-600 (#0D9488), secondary variant uses bg-[#F4F3F0] with text-[#1C1C1A]"
    - "Landing page hero headline reads 'Land interviews with one search.' per UI-SPEC"
    - "Vitest test run reports no tests from .claude/worktrees/** directories"
    - "All 6 button variant tests pass"
  artifacts:
    - path: "app/globals.css"
      provides: "Warm light theme CSS tokens"
      contains: "--color-warm-white"
    - path: "components/ui/Button.tsx"
      provides: "Button with spec-correct variants"
      contains: "bg-teal-600"
    - path: "vitest.config.ts"
      provides: "Test config excluding worktree directories"
      contains: ".claude/worktrees"
  key_links:
    - from: "app/globals.css @theme"
      to: "components/ui/Button.tsx"
      via: "Tailwind CSS token resolution"
      pattern: "warm-white|warm-muted"
---

<objective>
Fix the warm light theme deviation and Vitest worktree pollution identified in VERIFICATION.md.

Purpose: The UI-SPEC mandates a warm, light "Notion/Teal aesthetic" (#FAFAF8 background, teal-600 primary). The implementation shipped a dark theme (#0A0A0A), which the spec explicitly forbids ("not dev-tool aesthetic"). This causes 2 button tests to fail and makes the product look wrong. Additionally, stale worktree tests pollute the test run with 6 false failures.

Output:
- globals.css with warm light theme tokens replacing dark theme tokens
- Button.tsx with spec-correct variant classes (bg-teal-600, bg-[#F4F3F0])
- Landing page hero headline corrected to UI-SPEC copy
- vitest.config.ts excluding .claude/worktrees/**
</objective>

<execution_context>
@C:/Users/wasif/OneDrive/Desktop/teehee/03 Projects/Korvo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/wasif/OneDrive/Desktop/teehee/03 Projects/Korvo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-VERIFICATION.md
@.planning/phases/01-foundation/01-UI-SPEC.md

<interfaces>
<!-- Color tokens required by UI-SPEC (Section: Color): -->
<!--
  Dominant (60%): #FAFAF8  → --color-warm-white    (page bg, hero bg)
  Secondary (30%): #F4F3F0 → --color-warm-muted     (cards, nav, modal surface)
  Accent (10%): #0D9488    → bg-teal-600 (Tailwind built-in)
  Text primary: #1C1C1A    → --color-text-primary
  Text secondary: #6B7280  → text-gray-500 (Tailwind built-in)
  Border: #E5E4E0          → --color-border
  Focus ring: #0D9488      → ring-teal-600
-->

<!-- Button variants required by UI-SPEC (Section: Component Inventory > Button): -->
<!--
  primary:     bg: #0D9488 (bg-teal-600)  text: white     hover: #0F766E (hover:bg-teal-700)
  secondary:   bg: #F4F3F0                text: #1C1C1A   border: #E5E4E0  hover: #E9E8E4
  ghost:       bg: transparent            text: #1C1C1A   hover: #F4F3F0 bg
  destructive: bg: #DC2626 (bg-red-600)   text: white     hover: #B91C1C (hover:bg-red-700)
  All sizes:   h-[44px] minimum (touch target)  rounded-lg  px-4
-->

<!-- Hero headline required by UI-SPEC (Section: Copywriting Contract > Landing Page): -->
<!-- "Land interviews with one search." -->

<!-- Current dark tokens to REMOVE from globals.css: -->
<!--
  --color-dark-bg: #0A0A0A
  --color-dark-card: #141414
  --color-dark-border: #262626
  --color-dark-text: #EDEDED
  --color-dark-muted: #A1A1A1
  .glass-card (remove — dark-specific)
  .glow-border (remove — dark-specific)
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace dark theme tokens with warm light tokens in globals.css</name>
  <files>app/globals.css</files>
  <action>
    Replace the entire contents of `app/globals.css` with the warm light theme. The file currently uses dark theme variables (#0A0A0A, #141414, #262626) that violate the UI-SPEC.

    New globals.css must:

    1. Keep `@import "tailwindcss";` as the first line.

    2. In `:root`, define warm light tokens:
    ```css
    :root {
      --color-warm-white: #FAFAF8;
      --color-warm-muted: #F4F3F0;
      --color-border: #E5E4E0;
      --color-text-primary: #1C1C1A;
      --color-accent: #0D9488;
    }
    ```

    3. In `@theme inline`, register custom tokens for Tailwind utility generation:
    ```css
    @theme inline {
      --color-warm-white: var(--color-warm-white);
      --color-warm-muted: var(--color-warm-muted);
      --color-border: var(--color-border);
      --color-text-primary: var(--color-text-primary);
      --color-accent: var(--color-accent);
      --font-sans: var(--font-geist-sans);
      --font-mono: var(--font-geist-mono);
    }
    ```

    4. Body rule uses warm-white background and dark text:
    ```css
    body {
      background: var(--color-warm-white);
      color: var(--color-text-primary);
      font-family: var(--font-geist-sans, system-ui, sans-serif);
      -webkit-font-smoothing: antialiased;
      -moz-osx-font-smoothing: grayscale;
    }
    ```

    5. Remove `.glow-border` and `.glass-card` — these are dark-theme-specific utilities with no purpose in the light theme.

    Do NOT add any new utility classes beyond what is listed above.
  </action>
  <verify>
    Inspect the file — confirm `--color-dark-bg` is absent, `--color-warm-white: #FAFAF8` is present, `body { background: var(--color-warm-white) }` is present.
  </verify>
  <done>globals.css contains only warm light tokens. No dark-bg, dark-card, dark-border, dark-text, dark-muted variables. .glass-card and .glow-border removed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix Button.tsx variants and hero headline; fix Vitest worktree exclusion</name>
  <files>components/ui/Button.tsx, app/page.tsx, vitest.config.ts</files>
  <behavior>
    Button tests (tests/ui/button.test.tsx — all 6 must pass):
    - Test 1: primary variant renders with bg-teal-600 class
    - Test 2: secondary variant renders with bg-[#F4F3F0] and text-[#1C1C1A] classes
    - Test 3: ghost variant renders with bg-transparent
    - Test 4: destructive variant renders with bg-red-600
    - Test 5: isLoading=true renders spinner SVG
    - Test 6: disabled prop sets disabled attribute

    Vitest behavior:
    - Running `npx vitest run` does NOT pick up any test files under .claude/worktrees/**
  </behavior>
  <action>
    **A. Fix components/ui/Button.tsx**

    Update `buttonVariants` cva definition. Replace the current dark-theme class strings with spec-correct values:

    Base classes — keep the existing base but update focus ring to use teal and remove dark-bg offset:
    ```
    "inline-flex items-center justify-center rounded-lg font-semibold",
    "transition-colors duration-150 ease-in-out",
    "focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-teal-600 focus-visible:ring-offset-2",
    "active:scale-[0.97]",
    "disabled:opacity-50 disabled:cursor-not-allowed disabled:pointer-events-none",
    ```

    Variants (per UI-SPEC):
    ```typescript
    primary: "bg-teal-600 text-white hover:bg-teal-700",
    secondary: "bg-[#F4F3F0] text-[#1C1C1A] border border-[#E5E4E0] hover:bg-[#E9E8E4]",
    ghost: "bg-transparent text-[#1C1C1A] hover:bg-[#F4F3F0]",
    destructive: "bg-red-600 text-white hover:bg-red-700",
    ```

    Sizes — enforce 44px minimum touch target (h-11 = 44px):
    ```typescript
    default: "h-11 px-4 text-sm",
    lg: "h-[54px] px-8 text-base",
    ```

    All other Button.tsx code (props interface, loading spinner, JSX) stays identical.

    **B. Fix app/page.tsx — hero headline only**

    Find the `<h1>` element (currently around line 117). Replace its text content. Current:
    ```
    Find the right people. <br />
    <span className="text-accent">Send the right email.</span>
    ```
    Replace with (per UI-SPEC Copywriting Contract):
    ```
    Land interviews with one search.
    ```
    Remove the `<br />` and the inner `<span>`. The h1 class stays as-is — the dark theme classes on the surrounding page (bg-dark-bg, text-dark-text) will be addressed separately and are out of scope for this task (Task 1 fixes the CSS tokens; the Tailwind utility names on the page will resolve correctly once globals.css exports the right values via @theme inline).

    NOTE: Do NOT attempt to refactor all dark theme class names on app/page.tsx — that is out of scope for this gap closure. Only fix the hero headline copy.

    **C. Fix vitest.config.ts**

    In the `test.coverage.exclude` array, add `.claude/worktrees/**`:
    ```typescript
    exclude: ["node_modules/", ".next/", "tests/", ".claude/worktrees/**"],
    ```

    Also add `exclude` at the top-level `test` object (not just coverage) to prevent test discovery in worktrees:
    ```typescript
    test: {
      globals: true,
      environment: "node",
      exclude: ["node_modules/**", ".next/**", ".claude/worktrees/**"],
      setupFiles: ["./tests/setup.ts"],
      coverage: {
        provider: "v8",
        reporter: ["text", "lcov"],
        exclude: ["node_modules/", ".next/", "tests/", ".claude/worktrees/**"],
      },
    },
    ```
  </action>
  <verify>
    <automated>npx vitest run tests/ui/button.test.tsx</automated>
  </verify>
  <done>All 6 button tests pass. Hero headline in app/page.tsx reads "Land interviews with one search." vitest.config.ts has .claude/worktrees/** in both test.exclude and test.coverage.exclude.</done>
</task>

</tasks>

<verification>
Full test suite passes with zero worktree pollution:

```
npx vitest run
```

Expected: all previously-passing tests still pass, the 2 button variant tests now pass (previously failing), and no tests from .claude/worktrees/** appear in the output.

Spot-check the landing page visually after `npm run dev`: background should be warm off-white (#FAFAF8), not near-black. Hero headline should read "Land interviews with one search."
</verification>

<success_criteria>
- `npx vitest run tests/ui/button.test.tsx` → 6/6 pass (was 4/6)
- `npx vitest run` → no tests from .claude/worktrees/** in output
- `app/globals.css` → contains `--color-warm-white: #FAFAF8`, no `--color-dark-bg`
- `components/ui/Button.tsx` → primary variant uses `bg-teal-600`, secondary uses `bg-[#F4F3F0]`
- `app/page.tsx` h1 → "Land interviews with one search." (no inner span, no br)
- `vitest.config.ts` → `.claude/worktrees/**` in test.exclude and coverage.exclude
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-08-SUMMARY.md`
</output>
